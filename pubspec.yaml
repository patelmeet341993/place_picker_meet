name: place_picker_meet
description: A Flutter plugin which provides 'Picking Place' using Google Maps widget.
version: 1.0.0
homepage: https://github.com/patelmeet341993/place_picker_meet

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  google_maps_flutter: ^2.2.6
  google_maps_flutter_web: ^0.4.0+8
  geolocator: ^9.0.2
  permission_handler: ^9.2.0
  provider: ^6.0.3
  google_maps_webservice: ^0.0.20-nullsafety.5
  google_api_headers: 1.3.0
  tuple: ^2.0.1
  http: ^0.13.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
